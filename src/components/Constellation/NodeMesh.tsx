/**
 * This is a placeholder file that re-exports the renamed component
 * to maintain backward compatibility with existing imports.
 */

import { NodesInstanced } from './NodesInstanced';

export default NodesInstanced;